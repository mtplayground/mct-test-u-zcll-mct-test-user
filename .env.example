# Optional local development settings.
#
# The default server binds to 0.0.0.0:8080 so it works inside containers.
# Open http://localhost:8080 in your browser; localhost is a secure context
# exception for camera APIs such as getUserMedia.
PORT=8080
HOST=0.0.0.0
